<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/profile.css">
    <script src="https://kit.fontawesome.com/543ade074f.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <section class="profile-sec ">
        <div class="profile-left">
            <img src="<%= user.profileImage ? user.profileImage : 'https://thumbs.dreamstime.com/z/default-avatar-profile-icon-vector-user-image-179582665.jpg?w=768' %>" class="" alt="" srcset="">
            <h1>Hello <%= user.name.first %>
            </h1>

            <!-- Button trigger modal -->
            <button type="button" class="btns" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
                View Profile
            </button>
            <!-- Modal -->
            <div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
                aria-labelledby="staticBackdropLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h1 class="modal-title fs-5" id="staticBackdropLabel">Modal title</h1>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            ...
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-primary">Understood</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="profile-right p-3">
            <form class=" py-2 d-flex flex-column justify-content-center align-items-center"
                action="/profile/update/<%= user.id %>" method="post">
                <div class="w-75 f-container mb-3">
                    <p>Please fill all the details to proceed to registration!</p>
                    <div class="row ">
                        <div class="col ">
                            <label for="firstName" class="form-label">First Name</label>
                            <input type="text" value="<%= user.name.first ? user.name.first : '' %>" name="firstName"
                                class="form-control" placeholder="First name" aria-label="First name">
                        </div>
                        <div class="col ">
                            <label for="lastName" class="form-label">Last Name</label>
                            <input type="text" value="<%= user.name.last ? user.name.last : '' %>" name="lastName"
                                class="form-control" placeholder="Last name" aria-label="Last name">
                        </div>
                    </div>
                    <div class="row ">
                        <div class="col ">
                            <label for="age" class="form-label">Age</label>
                            <input type="number" value="<%= user.age ? user.age : '' %>" name="age" class="form-control"
                                placeholder="Age" aria-label="Age">
                        </div>
                        <div class="col ">
                            <label for="gender" class="form-label">Gender</label>
                            <input type="text" value="<%= user.gender ? user.gender : '' %>" name="gender"
                                class="form-control" placeholder="Gender" aria-label="Gender">
                        </div>
                    </div>
                    <div class="row ">
                        <div class="col ">
                            <label for="exampleInputEmail1" class="form-label">Email address</label>
                            <input type="email" value="<%= user.email ? user.email : '' %>" class="form-control"
                                name="email" placeholder="Email" id="exampleInputEmail1" aria-describedby="emailHelp">
                        </div>
                        <div class="col ">
                            <label for="phoneNumer" class="form-label">Phone No</label>
                            <input type="number" value="<%= user.phone ? user.phone : '' %>" name="phoneNumer"
                                class="form-control" placeholder="Phone Number" aria-label="Phone number">
                        </div>
                    </div>
                    <div class="row ">
                        <div class="col mb-3">
                            <label for="state" class="form-label">State</label>
                            <select name="state" class="form-control" id="">
                                <option disabled <% if (!user.address.state) { %>selected<% } %>>Select state</option>
                                <option value="assam" <% if (user.address.state==='assam' ) { %>selected<% } %>>Assam
                                </option>
                                <option value="chandigarh" <% if (user.address.state==='chandigarh' ) { %>selected<% }
                                        %>>Chandigarh</option>
                                <option value="gujrat" <% if (user.address.state==='gujrat' ) { %>selected<% } %>>Gujrat
                                </option>
                                <option value="maharashtra" <% if (user.address.state==='maharashtra' ) { %>selected<% }
                                        %>>Maharashtra</option>
                            </select>

                        </div>
                        <div class="col mb-3">
                            <label for="district" class="form-label">District</label>
                            <select name="district" class="form-control" id="">
                                <option disabled <% if (!user.address.district) { %>selected<% } %>>Select district
                                </option>
                                <option value="akola" <% if (user.address.district==='akola' ) { %>selected<% } %>>Akola
                                </option>
                                <option value="amravati" <% if (user.address.district==='amravati' ) { %>selected<% } %>
                                        >Amravati</option>
                                <option value="chandrapur" <% if (user.address.district==='chandrapur' ) { %>selected<%
                                        } %>>Chandrapur</option>
                                <option value="dhule" <% if (user.address.district==='dhule' ) { %>selected<% } %>>Dhule
                                </option>
                                <option value="nashik" <% if (user.address.district==='nashik' ) { %>selected<% } %>
                                        >Nashik</option>
                            </select>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col ">
                            <label for="street" class="form-label">Address</label>
                            <textarea name="street" class="form-control" placeholder="Street" id="" cols="20"
                                rows="2"><%= user.address.street ? user.address.street : '' %></textarea>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col ">
                            <label for="profileUrl" class="form-label">Profile Url</label>
                            <input type="url" value="<%= user.profileImage ? user.profileImage : '' %>"
                                placeholder="Profile Url" class="form-control" name="profileUrl" id="">
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary btns">Update Profile</button>
            </form>
        </div>
    </section>
</body>

</html>